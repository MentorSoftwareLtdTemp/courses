define(function (require) {
    //var $=require("jquery");
    var dashboard=require('dashboard');

    var bootstrap = require('bootstrap');
    //var f = require('fuelux/all');
    console.log(bootstrap, $);
    console.log($.fn);
    $( document ).ready(function() {

    });

});